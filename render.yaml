services:
  - type: web
    name: arbuda-fashion
    env: node
    rootDir: ./
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: MONGODB_URI
        sync: false  # Set this manually in Render dashboard
      - key: NODE_ENV
        value: production
    autoDeploy: true

